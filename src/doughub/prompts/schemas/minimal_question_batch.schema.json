{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MinimalQuestionBatch",
  "description": "Schema for validating LLM-extracted minimal question data",
  "type": "object",
  "required": ["questions"],
  "properties": {
    "questions": {
      "type": "array",
      "description": "Array of extracted questions",
      "items": {
        "type": "object",
        "required": ["question_stem_html"],
        "properties": {
          "question_context_html": {
            "type": "string",
            "description": "Optional context HTML for the question (can be empty string)"
          },
          "question_stem_html": {
            "type": "string",
            "description": "Required HTML content of the question stem",
            "minLength": 1
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
